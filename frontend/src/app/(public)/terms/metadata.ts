import { createGenericMetadata } from "@/lib/utils/seo";

/**
 * 利用規約用のmetadata
 */
export const termsMetadata = createGenericMetadata({
  title: "利用規約",
  description: "サイトの利用規約に関する方針を記載しています。",
  pathname: "/terms",
});
